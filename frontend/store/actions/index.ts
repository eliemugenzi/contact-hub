import { setCurrentUser, clearCurrentUser } from "./auth";
import { getContacts, searchContacts } from './contacts'

export default {
    setCurrentUser,
    clearCurrentUser,
    getContacts,
    searchContacts,
}